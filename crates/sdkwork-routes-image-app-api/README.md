# sdkwork-routes-image-app-api

This README is the SDKWork module entrypoint for `sdkwork-routes-image-app-api`. The machine-readable component contract is `specs/component.spec.json`; canonical standards are under `../../../../sdkwork-specs/`.

This crate owns the image app-api route manifest for the `/app/v3/api` surface and materializes the user-facing image contract.
