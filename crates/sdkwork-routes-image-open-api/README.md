# sdkwork-routes-image-open-api

This README is the SDKWork module entrypoint for `sdkwork-routes-image-open-api`. The machine-readable component contract is `specs/component.spec.json`; canonical standards are under `../../../../sdkwork-specs/`.

This crate owns the image open-api route manifest for the `/image/v3/api` surface and materializes the public image compatibility contract.
