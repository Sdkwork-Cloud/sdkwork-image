# sdkwork_image_generation_repository_sqlx

This README is the SDKWork module entrypoint for `sdkwork_image_generation_repository_sqlx`. The machine-readable component contract is `specs/component.spec.json`; canonical standards are under `../../../../sdkwork-specs/`.

This crate owns image database tables, migrations, and repository SQL contracts for generation jobs, multi-output records, provider tasks, webhook events, Drive sync status, and notification outbox.
