# sdkwork-router-image-backend-api

This README is the SDKWork module entrypoint for `sdkwork-router-image-backend-api`. The machine-readable component contract is `specs/component.spec.json`; canonical standards are under `../../../../sdkwork-specs/`.

This crate owns the image backend-api route manifest for the `/backend/v3/api` surface and materializes the operator/admin image contract.
